.pt-3
  - if controller_name != 'sessions'
    = link_to t('views.buttons.login'), new_session_path(resource_name)
    %br/
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to t('views.buttons.signup'), new_registration_path(resource_name)
    %br/
